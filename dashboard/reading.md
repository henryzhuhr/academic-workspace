# Reading Queue

本文件维护近期阅读行动，不替代 `literature/` 中的正式笔记。完成阅读后，将稳定内容写入阅读笔记或主题综述，并从队列移除。

| Item | Status | Priority | Related Projects | Next Action | Updated |
| --- | --- | --- | --- | --- | --- |

状态建议使用 `queued`、`reading`、`extracting`、`done` 或 `dropped`；项目引用使用稳定的项目 `id`。
