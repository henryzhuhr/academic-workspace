# Archive

顶层 `archive/` 只保存不属于具体项目的历史材料、已停止的工作区级方案或尚未确定归属的旧内容。

完整研究项目不移动到这里。项目结束后保留在原来的 `projects/<slug>/`，将 `project.json.status` 更新为 `archived`，填写最终状态和复盘，从而保持所有链接稳定。

项目内部的旧方案、废弃草稿和阶段版本进入该项目自己的 `archive/`。

归档不等于删除。归档材料应保留来源、最终状态、重要输出、停止原因以及可能的恢复条件。
